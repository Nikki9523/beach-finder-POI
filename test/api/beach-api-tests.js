import { assert } from "chai";
import { assertSubset } from "../test-utils.js";
import { beachfinderService } from "./beachfinder-service.js";
import { maggie, testCategories, testBeaches } from "../fixtures.js";

suite("beach API tests", () => {
  let user = null;

  setup(async () => {
  });

  teardown(async () => {});

  test("create beach", async () => {
  });

  test("create Multiple beaches", async () => {
  });

  test("Delete beach", async () => {
  });

  test("test denormalised category", async () => {
  });
});